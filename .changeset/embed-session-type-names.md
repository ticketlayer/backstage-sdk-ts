---
"@ticketlayer/backstage": patch
---

Export the embed session types under the spec's names, `EmbedSessionCreated` and `CurrentEmbedSession`, from the package entry point; `EmbedSession` and `EmbedSessionInfo` remain as deprecated aliases.
